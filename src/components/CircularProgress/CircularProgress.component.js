import React from 'react';
import BaseCircularProgress from '../Base/BaseCircularProgress';

const CircularProgress = (props) => {
  return <BaseCircularProgress {...props} />;
};

export default CircularProgress;
